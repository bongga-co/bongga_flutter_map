name: bongga_flutter_map
description: A new Flutter package to display a custom flutter_map
publish_to: 'none'
version: 0.0.7
author: Bongga
homepage: https://bongga.co

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.25.0
  geolocator: ^5.1.3
  flutter_map:
    git:
      url: https://github.com/bongga-co/flutter_map.git

dev_dependencies:
  flutter_test:
    sdk: flutter
